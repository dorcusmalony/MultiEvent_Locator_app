DATABASE_NAME=event_locator
DATABASE_USER=postgres
DATABASE_PASSWORD=your_password
DATABASE_HOST=localhost
JWT_SECRET=your_jwt_secret
PORT=3000
node_modules/
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# Configure the master node for replication
replicaof <masterip> <masterport>

# Authenticate with the master node
# masterauth your_master_password # Commented out since no password is required

# Optional: Specify a special user for replication (Redis 6+)
masteruser replication_user